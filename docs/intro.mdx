import { Title } from "@/components/pages/Title";
import { Stat } from "@/components/pages/StatusJSON";
import { Status } from "@/components/pages/Status";
import { BMFView } from "@/components/pages/bmf";

export const metadata = {
	title: "Introduction",
	description: "All the information you'll ever need!"
};

<Title />

# MDX in Next.js

### Custom components with React Hooks

`<Stat jsonEntry="numBans" defaultValue="0" />` ➡️ <Stat jsonEntry="numBans" defaultValue="0" />

`<Status/>` ➡️ ...
<Status/>

### Codeblocks rendered with Shiki

```luau

-- Luau

local owner: Player = game:GetService("Players"):FindFirstChild("OCboy3")
```

```mdx-ts

// TypeScript

const x = 1;
```

### Standalone React components

<BMFView/>