import { Title } from "@/components/pages/Title";
import { Stat } from "@/components/pages/StatusJSON";
import { Status } from "@/components/pages/Status";

export const metadata = {
	title: "Introduction",
	description: "All the information you'll ever need!"
};

Well.. Here we go...

<Title/>

# This is MDX
## MDX
### Next.JS!

We have banned <Stat jsonEntry="numBans" defaultValue="0" /> users with 112

<Status/>

This data is fetched with a React hook.

> This is a blockquote

```luau

-- Luau

local owner: Player = game:GetService("Players"):FindFirstChild("OCboy3")
```

```mdx-ts

// TypeScript

export const owner: Player = await getPlayer();
```

```mdx-cpp

// C++

#include <iostream>

int main() {
    std::cout << "Hello World!";
    return 0;
}
```
